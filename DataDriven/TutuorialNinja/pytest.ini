[pytest]
markers =
    search: marks tests as search tests (deselect with '-m "not search"')
    order: controls test execution order (via pytest-order)
    depends: declares test dependencies (via pytest-depends)